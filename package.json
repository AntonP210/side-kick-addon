{
  "name": "sidekick",
  "version": "1.0.0",
  "description": "Highlight text, right-click, and instantly search, translate, or navigate — works on Chrome and Firefox",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:chrome": "tsc && vite build --mode chrome",
    "build:firefox": "tsc && vite build --mode firefox",
    "package": "node scripts/package.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/webextension-polyfill": "^0.12.1",
    "@vitejs/plugin-react": "^4.3.0",
    "archiver": "^7.0.1",
    "decode-ico": "^0.4.1",
    "sharp": "^0.34.5",
    "typescript": "^5.7.0",
    "vite": "^5.4.0",
    "vitest": "^4.0.18"
  }
}
